Testing git push.
This is how to git config, clone, add, commit and push in Git Bush:

$ cd
$ cd /DRIVER
$ ls
$ cd LIBRARY/
$ git config --global user.name "YOUR USERNAME"
$ git config --global user.email "YOUR EMAIL"

$ git clone URL
$ ls
$ cd LYBRARY/

$ ls
$ git add FILENAME.FILEFORMAT
$ git commit -m "COMMITTED MESSAGE" FILENAME
$ git push -u origin master